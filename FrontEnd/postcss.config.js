export default {
    plugins: {
        "@tailwindcss/postcss": {},
        autoprefixer: {}, // autoprefixer is actually included in @tailwindcss/postcss usually but can be kept or removed. The docs say just @tailwindcss/postcss is enough.
    },
}
